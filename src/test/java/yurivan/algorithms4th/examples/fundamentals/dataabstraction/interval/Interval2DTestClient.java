package yurivan.algorithms4th.examples.fundamentals.dataabstraction.interval;

public class Interval2DTestClient {
    public static void main(String[] args) {
        /*
        double xlo = Double.parseDouble(args[0]);
        double xhi = Double.parseDouble(args[0]);
        double ylo = Double.parseDouble(args[2]);
        double yhi = Double.parseDouble(args[3]);

        final int times = Integer.parseInt(args[0]);

        Interval1D xInterval = new Interval1D(xlo, xhi);
        Interval1D yInterval = new Interval1D(ylo, yhi);
        Interval2D box = new Interval2D(xInterval, yInterval);
        box.draw();

        Counter hits = new Counter("hits");
        for (int t = 0; t < times; ++t) {
            final double x = Math.random();
            final double y = Math.random();

            Point2D point = new Point2D(x, y);
            if (box.contains(point)) {
                hits.increment();
            } else {
                point.draw();
            }
        }

        StdOut.println(hits);
        StdOut.println(box.area());
         */
    }
}
